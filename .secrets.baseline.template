# Template baseline for detect-secrets
# Generate your actual baseline locally so that detect-secrets knows which findings are intentional.
# To create: `detect-secrets scan > .secrets.baseline` (install detect-secrets via pip or pre-commit)
{
  "results": {}
}
